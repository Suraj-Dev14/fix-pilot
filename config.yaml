project:
  name: fixpilot-demo

service:
  name: product-service

environment: production

integrations:
  logs: demo

  source_control:
    provider: local_git
    repository_path: .

  deployments: demo
  api: demo
  regression: demo

incident:
  default_severity: medium